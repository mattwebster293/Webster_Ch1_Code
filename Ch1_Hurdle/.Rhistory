##########################################################################
# RANDOM EFFECT PRIORS FOR SW AMOUNT:
for(j in 1:n_stream){
beta_stream[j] ~ dnorm(0, tau_beta_stream)
eta_stream[j]  ~ dnorm(0, tau_eta_stream)
}
for(yr in 1:n_year){
beta_year[yr] ~ dnorm(0, tau_beta_year)
eta_year[yr] ~ dnorm(0, tau_eta_year)
}
for(k in 1:n_event){
beta_event[k] ~ dnorm(beta_year[event_year[k]], tau_beta_event)
eta_event[k]  ~ dnorm(eta_year[event_year[k]], tau_eta_event)
}
for(c in 1:n_cluster){
eta_cluster[c] ~ dnorm(0, tau_eta_cluster)
}
##########################################################################
# DETECTION PRIORS:
for(k in 1:n_event){
p_TP[k] ~ dnorm(0.76, pow(0.16,-2)) T(0,1)   # empirically derived recall
p_FP[k] ~ dnorm(0.01, pow(0.005,-2)) T(0,1)  # empirically derived FPR
}
##########################################################################
# MAIN LOOP: PRESENCE, DETECTION, AMOUNT
for(i in 1:N){
# Presence/absence:
z[i] ~ dbern(psi[i])
logit(psi[i]) <- epsilon
+ (zeta_damPres
+ zeta_stream[stream[i]]
+ zeta_year[year[i]]
+ zeta_event[event[i]]) * damPres[i]
+ iota_stream[stream[i]]
+ iota_event[event[i]]
+ iota_cluster[cluster[i]]
# Detection:
p_eff[i] <- z[i] * p_TP[event[i]] + (1-z[i]) * p_FP[event[i]]
y[i]     ~ dbern(p_eff[i])
# Amount:
y2[i] ~ dnorm(mu[i], tau_eff[i])
mu[i] <- z[i] *
(alpha
+ (beta_damPres
+ beta_stream[stream[i]]
+ beta_year[year[i]]
+ beta_event[event[i]]) * damPres[i]
+ eta_stream[stream[i]]
+ eta_event[event[i]]
+ eta_cluster[cluster[i]])
tau_eff[i] <- z[i] * tau + (1 - z[i]) * 100
}
}
", fill = TRUE)
sink()
hurdle_damPres_m1_data_list <- list(
N = N,
y = y,
y2 = y2,
stream = stream,
year = year,
event = event,
cluster = cluster,
damPres = damPres,
incis = as.numeric(scale(1-(aoi_HurdleData$TO/90))), # Channel Incision
RIPA = as.numeric(scale(aoi_HurdleData$riparian_area_m2)),
UDA = as.numeric(scale(aoi_HurdleData$catchment_km2)),
n_stream = n_stream,
n_year = n_year,
n_event = n_event,
n_cluster = n_cluster,
event_year = event_year_3_4
)
hurdle_damPres_m1_inits <- function(){
list(sigma ~ dnorm(0, 0.04)) # mean 0 with 5 SD spread (very diffuse prior)
}
hurdle_damPres_m1_params <- c(
"psi",
"mu",
"epsilon",
"alpha",
"zeta_damPres",
"zeta_incis",
"zeta_RIPA",
"zeta_UDA",
"zeta_stream",
"zeta_year",
"zeta_event",
"iota_stream",
"iota_event",
"iota_cluster",
"beta_damPres",
"beta_incis",
"beta_RIPA",
"beta_UDA",
"beta_stream",
"beta_year",
"beta_event",
"eta_stream",
"eta_year",
"eta_event",
"eta_cluster",
"sigma",
"sigma_zeta_stream",
"sigma_zeta_year",
"sigma_zeta_event",
"sigma_iota_stream",
"sigma_iota_event",
"sigma_iota_cluster",
"sigma_beta_stream",
"sigma_beta_year",
"sigma_beta_event",
"sigma_eta_stream",
"sigma_eta_year",
"sigma_eta_event",
"sigma_eta_cluster",
"p_TP",
"p_FP",
"z",
"tau_eff"
)
nchains <- 3
nthin <- 5
niter <- 100000
nburn <- 25000
Sys.time()
hurdle_damPres_m1_SW <- jags(data = hurdle_damPres_m1_data_list,
inits = hurdle_damPres_m1_inits,
parameters.to.save = hurdle_damPres_m1_params,
model.file = "HGLMM_Hurdle_m1_SW_damPres.jags",
parallel = T,
n.chains = nchains,
n.thin = nthin,
n.iter = niter,
n.burnin = nburn)
hurdle_damPres_m1_data_list <- list(
N = N,
y = y,
y2 = y2,
stream = stream,
year = year,
event = event,
cluster = cluster,
damPres = damPres,
incis = as.numeric(scale(1-(aoi_HurdleData$TO/90))), # Channel Incision
RIPA = as.numeric(scale(aoi_HurdleData$riparian_area_m2)),
UDA = as.numeric(scale(aoi_HurdleData$catchment_km2)),
n_stream = n_stream,
n_year = n_year,
n_event = n_event,
n_cluster = n_cluster,
event_year = event_year_3_4
)
hurdle_damPres_m1_inits <- function(){
list()
}
hurdle_damPres_m1_params <- c(
"psi",
"mu",
"epsilon",
"alpha",
"zeta_damPres",
"zeta_incis",
"zeta_RIPA",
"zeta_UDA",
"zeta_stream",
"zeta_year",
"zeta_event",
"iota_stream",
"iota_event",
"iota_cluster",
"beta_damPres",
"beta_incis",
"beta_RIPA",
"beta_UDA",
"beta_stream",
"beta_year",
"beta_event",
"eta_stream",
"eta_year",
"eta_event",
"eta_cluster",
"sigma",
"sigma_zeta_stream",
"sigma_zeta_year",
"sigma_zeta_event",
"sigma_iota_stream",
"sigma_iota_event",
"sigma_iota_cluster",
"sigma_beta_stream",
"sigma_beta_year",
"sigma_beta_event",
"sigma_eta_stream",
"sigma_eta_year",
"sigma_eta_event",
"sigma_eta_cluster",
"p_TP",
"p_FP",
"z",
"tau_eff"
)
nchains <- 3
nthin <- 5
niter <- 100000
nburn <- 25000
Sys.time()
hurdle_damPres_m1_SW <- jags(data = hurdle_damPres_m1_data_list,
inits = hurdle_damPres_m1_inits,
parameters.to.save = hurdle_damPres_m1_params,
model.file = "HGLMM_Hurdle_m1_SW_damPres.jags",
parallel = T,
n.chains = nchains,
n.thin = nthin,
n.iter = niter,
n.burnin = nburn)
Sys.time()
# Plotting specifications from Rmarkdown.
par(mfrow = c(2, 2),        # 2 rows, 2 columns
mar = c(4, 4, 2, 1),    # smaller margins
cex = 0.8)              # shrink text/points a bit
#Extract posteriors
damPres_m2_post <- hurdle_damPres_m2_SW$sims.list
# Plotting specifications from Rmarkdown.
par(mfrow = c(2, 2),        # 2 rows, 2 columns
mar = c(4, 4, 2, 1),    # smaller margins
cex = 0.8)              # shrink text/points a bit
#Extract posteriors
damPres_m1_post <- hurdle_damPres_m1_SW$sims.list
# Choose only the parameters you care about
params_m1 <- c(
"epsilon",
"alpha",
"zeta_damPres",
"beta_damPres",
"sigma",
"sigma_zeta_stream",
"sigma_iota_cluster",
"p_TP[1]",
"p_FP[1]")
# Gelman–Rubin convergence diagnostic
gelman.diag(hurdle_damPres_m1_SW$samples[, params_m1], multivariate = FALSE)
# Effective sample size
effectiveSize(hurdle_damPres_m1_SW$samples[, params_m1])
# Choose only the parameters you care about
params_m1 <- c(
"zeta_stream", "zeta_year", "zeta_event",
"iota_stream", "iota_year", "iota_event", "iota_cluster",
"beta_stream", "beta_year", "beta_event",
"eta_stream", "eta_year", "eta_event", "eta_cluster",
"sigma_zeta_year", "sigma_zeta_event",
"sigma_iota_year", "sigma_iota_event",
"sigma_beta_year", "sigma_beta_event",
"sigma_eta_year", "sigma_eta_event"
)
# Gelman–Rubin convergence diagnostic
gelman.diag(hurdle_damPres_m1_SW$samples[, params_m1])
# Choose only the parameters you care about
params_m1 <- c(
"zeta_stream", "zeta_year", "zeta_event",
"iota_stream", "iota_year", "iota_event", "iota_cluster",
"beta_stream", "beta_year", "beta_event",
"eta_stream", "eta_year", "eta_event", "eta_cluster",
"sigma_zeta_year", "sigma_zeta_event",
"sigma_iota_year", "sigma_iota_event",
"sigma_beta_year", "sigma_beta_event",
"sigma_eta_year", "sigma_eta_event"
)
# Gelman–Rubin convergence diagnostic
gelman.diag(hurdle_damPres_m1_SW$samples[, params_m1])
# Choose only the parameters you care about
params_m1 <- c(
"zeta_stream"
)
# Gelman–Rubin convergence diagnostic
gelman.diag(hurdle_damPres_m1_SW$samples[, params_m1])
# Choose only the parameters you care about
params_m1 <- c(
"epsilon",
"alpha",
"zeta_damPres",
"beta_damPres",
"sigma",
"sigma_zeta_stream",
"sigma_iota_cluster",
"p_TP[1]",
"p_FP[1]")
# Gelman–Rubin convergence diagnostic
gelman.diag(hurdle_damPres_m1_SW$samples[, params_m1], multivariate = FALSE)
# Effective sample size
effectiveSize(hurdle_damPres_m1_SW$samples[, params_m1])
# Choose only the parameters you care about
params_m1 <- c(
"epsilon",
"alpha",
"zeta_damPres",
"beta_damPres",
"sigma",
"sigma_zeta_stream",
"sigma_zeta_year",
"sigma_zeta_event",
"sigma_iota_stream",
"sigma_iota_year",
"sigma_iota_event",
"sigma_iota_cluster",
"p_TP[1]",
"p_FP[1]")
# Gelman–Rubin convergence diagnostic
gelman.diag(hurdle_damPres_m1_SW$samples[, params_m1], multivariate = FALSE)
# Choose only the parameters you care about
params_m1 <- c(
"epsilon",
"alpha",
"zeta_damPres",
"beta_damPres",
"sigma",
"sigma_zeta_stream",
"sigma_zeta_year",
"sigma_zeta_event",
"sigma_iota_stream",
"sigma_iota_year",
"sigma_iota_event",
"sigma_iota_cluster",
"p_TP[1]",
"p_FP[1]")
# Gelman–Rubin convergence diagnostic
gelman.diag(hurdle_damPres_m1_SW$samples[, params_m1], multivariate = FALSE)
# Gelman–Rubin convergence diagnostic
gelman.diag(hurdle_damPres_m1_SW$samples[, params_m1])
names(hurdle_damPres_m1_SW$samples)
str(hurdle_damPres_m1_SW$samples)
View(hurdle_damPres_m1_SW)
hurdle_damPres_m1_SW$samples[,parameters]
# Choose only the parameters you care about
params_m1 <- c(
"epsilon",
"alpha",
"zeta_damPres",
"beta_damPres",
"sigma",
"sigma_zeta_stream",
"sigma_iota_stream",
"p_TP[1]",
"p_FP[1]")
# Gelman–Rubin convergence diagnostic
gelman.diag(hurdle_damPres_m1_SW$samples[, params_m1])
# Effective sample size
effectiveSize(hurdle_damPres_m1_SW$samples[, params_m1])
# Choose only the parameters you care about
params_m1 <- c(
"epsilon",
"alpha",
"zeta_damPres",
"beta_damPres",
"sigma",
"sigma_zeta_stream",
"sigma_zeta_year",
"sigma_iota_stream",
"sigma_iota_event",
"p_TP[1]",
"p_FP[1]")
# Gelman–Rubin convergence diagnostic
gelman.diag(hurdle_damPres_m1_SW$samples[, params_m1])
# Effective sample size
effectiveSize(hurdle_damPres_m1_SW$samples[, params_m1])
# Choose only the parameters you care about
params_m1 <- c(
"epsilon",
"alpha",
"zeta_damPres",
"beta_damPres",
"sigma",
"sigma_zeta_stream",
"sigma_zeta_year",
"sigma_zeta_event",
"sigma_iota_stream",
"sigma_iota_event",
"sigma_iota_cluster",
"p_TP[1]",
"p_FP[1]")
# Gelman–Rubin convergence diagnostic
gelman.diag(hurdle_damPres_m1_SW$samples[, params_m1])
# Effective sample size
effectiveSize(hurdle_damPres_m1_SW$samples[, params_m1])
# convert each stream's samples into mcmc.list
zeta_stream_mcmc <- lapply(1:dim(zeta_stream)[3], function(j) {
mcmc.list(
mcmc(zeta_stream[,1,j]),
mcmc(zeta_stream[,2,j]),
mcmc(zeta_stream[,3,j])
)
})
# convert each stream's samples into mcmc.list
zeta_stream_mcmc <- lapply(1:dim(hurdle_damPres_m1_SW$samples[,"zeta_stream"])[3], function(j) {
mcmc.list(
mcmc(zeta_stream[,1,j]),
mcmc(zeta_stream[,2,j]),
mcmc(zeta_stream[,3,j])
)
})
hurdle_damPres_m1_SW[["parameters"]]
# convert each stream's samples into mcmc.list
zeta_stream_mcmc <- lapply(1:dim(hurdle_damPres_m1_SW$samples[,"zeta_stream"])[], function(j) {
mcmc.list(
mcmc(zeta_stream[,1,j]),
mcmc(zeta_stream[,2,j]),
mcmc(zeta_stream[,3,j])
)
})
dim(hurdle_damPres_m1_SW$samples$zeta_stream)
dim(hurdle_damPres_m1_SW$samples[,"zeta_stream"])
hurdle_damPres_m1_data_list <- list(
N = N,
y = y,
y2 = y2,
stream = stream,
year = year,
event = event,
cluster = cluster,
damPres = damPres,
incis = as.numeric(scale(1-(aoi_HurdleData$TO/90))), # Channel Incision
RIPA = as.numeric(scale(aoi_HurdleData$riparian_area_m2)),
UDA = as.numeric(scale(aoi_HurdleData$catchment_km2)),
n_stream = n_stream,
n_year = n_year,
n_event = n_event,
n_cluster = n_cluster,
event_year = event_year_3_4
)
hurdle_damPres_m1_inits <- function(){
list()
}
hurdle_damPres_m1_params <- c(
"psi",
"mu",
"epsilon",
"alpha",
"zeta_damPres",
"zeta_stream",
"zeta_year",
"zeta_event",
"iota_stream",
"iota_event",
"iota_cluster",
"beta_damPres",
"beta_stream",
"beta_year",
"beta_event",
"eta_stream",
"eta_year",
"eta_event",
"eta_cluster",
"sigma",
"sigma_zeta_stream",
"sigma_zeta_year",
"sigma_zeta_event",
"sigma_iota_stream",
"sigma_iota_event",
"sigma_iota_cluster",
"sigma_beta_stream",
"sigma_beta_year",
"sigma_beta_event",
"sigma_eta_stream",
"sigma_eta_year",
"sigma_eta_event",
"sigma_eta_cluster",
"p_TP",
"p_FP",
"z",
"tau_eff"
)
nchains <- 3
nthin <- 5
niter <- 100000
nburn <- 25000
Sys.time()
hurdle_damPres_m1_SW <- jags(data = hurdle_damPres_m1_data_list,
inits = hurdle_damPres_m1_inits,
parameters.to.save = hurdle_damPres_m1_params,
model.file = "HGLMM_Hurdle_m1_SW_damPres.jags",
parallel = T,
n.chains = nchains,
n.thin = nthin,
n.iter = niter,
n.burnin = nburn)
Sys.time()
# Choose only the parameters you care about
params_m1 <- c(
"epsilon",
"alpha",
"zeta_damPres",
"beta_damPres",
"sigma",
"sigma_zeta_stream",
"sigma_zeta_year",
"sigma_zeta_event",
"sigma_iota_stream",
"sigma_iota_event",
"sigma_iota_cluster",
"p_TP[1]",
"p_FP[1]")
# Gelman–Rubin convergence diagnostic
gelman.diag(hurdle_damPres_m1_SW$samples[, params_m1])
# Effective sample size
effectiveSize(hurdle_damPres_m1_SW$samples[, params_m1])
# convert each stream's samples into mcmc.list
zeta_stream_mcmc <- lapply(1:dim(hurdle_damPres_m1_SW$samples[,"zeta_stream"])[], function(j) {
mcmc.list(
mcmc(zeta_stream[,1,j]),
mcmc(zeta_stream[,2,j]),
mcmc(zeta_stream[,3,j])
)
})
